/**
 * @file misc.h
 * @author Charles Averill
 * @brief Miscellaneous utility functions that don't fit anywhere else
 * @date 07-Jun-2023
 */

#ifndef MISC_H
#define MISC_H

#include <math.h>
#include <stdio.h>
#include <string.h>

#define MIN(x, y) (x < y ? x : y)
#define MAX(x, y) (x > y ? x : y)

#endif /* MISC_H */
