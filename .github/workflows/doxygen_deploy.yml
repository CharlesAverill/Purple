name: Doxygen GitHub Pages Deploy Action

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: mattnotmitt/doxygen-action@v1.9.4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          doxyfile-path: .doxygen_cfg
